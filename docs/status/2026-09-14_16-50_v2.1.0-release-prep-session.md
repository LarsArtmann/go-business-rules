# Status — v2.1.0 release-prep session (2026-09-14)

Scope: the entire TODO_LIST at session start (CI/CI-billing, version tags,
branching-flow pins, Polish-Customs, json/v2, and all events/streaming
follow-ups). Every item is resolved or explicitly blocked on a user action.

## What shipped

| Item                                                                                                                                                        | Where                                                         |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| `WithConcurrency(n)` for `Stream` (semaphore, cancellation-aware)                                                                                           | `validator.go` (`scheduleRules`, `evaluateRuleAt`)            |
| `ContextRule` additive interface + `NewContextRule` builder                                                                                                 | `rule.go`, used by `evaluateRule` in `Stream`                 |
| Goroutine-leak regression specs (abandoned + drained)                                                                                                       | `stream_test.go`                                              |
| Stream benchmarks (2 rules / 10 rules / 10+concurrency 4)                                                                                                   | `benchmark_test.go`                                           |
| `listeners/otel` nested module (spans + 5 metric instruments, sdk-tested)                                                                                   | `listeners/otel/`                                             |
| `examples/sse` rewritten as a Datastar feed (patches as values → same `go-sse` Broadcaster; SSE is the `@post` response per current datastar v1.2 protocol) | `examples/sse/main.go`                                        |
| `check-all` flake app (build+vet+test+lint × 4 modules)                                                                                                     | `flake.nix` → `nix run .#check-all`                           |
| Module path → `github.com/LarsArtmann/go-business-rules/v2`, tag `v2.1.0` cut locally                                                                       | `go.mod`, `doc.go`, nested `go.mod`s, `.golangci.yml`, README |
| Branching-flow policy applied: re-pin to analyzer reality (18 PHANTOM / 22 stats; panic FP suppressed via `//nolint:branching-flow:panic`)                  | `bdd_branching_flow_test.go`, `validator.go`                  |
| CI workflow rewritten as 4-module matrix; root cause of "setup failures" identified as GitHub billing rejection (run `29447520877`)                         | `.github/workflows/ci.yml`                                    |
| Polish-Customs migrated to `/v2` (temporary local `replace`), full suite green                                                                              | `~/projects/Polish-Customs/go.mod`                            |
| `.buildflow.yml` with curated `skip_steps` + `GOEXPERIMENT` env                                                                                             | `.buildflow.yml`                                              |

## Verification performed

- `nix run .#check-all` → `ALL MODULES GREEN` (build, vet, test, lint × 4 modules).
- Root: 169/169 Ginkgo specs (incl. branching-flow pins), coverage 95.9%,
  `go test -race -count=1` clean.
- gosec (local, `GOEXPERIMENT=jsonv2`): clean.
- Consumability: module zip built from the final tree with `x/mod/zip`, served
  via a local file proxy; an external consumer resolved
  `github.com/LarsArtmann/go-business-rules/v2@v2.1.0`, compiled, and ran it
  (`2.1.0 false true`).
- Polish-Customs full suite (10 packages) green against the local `/v2` tree.

## Blocked on user actions (recorded in TODO_LIST.md)

1. `git push origin master && git push origin v2.1.0` — pushes require explicit
   approval; nothing is pushed by this session.
2. After push: drop the temporary `replace` in Polish-Customs `go.mod`.
3. Fix GitHub Actions billing, then `gh workflow enable CI`.

## Open by design

- `encoding/json/v2` still requires `GOEXPERIMENT=jsonv2` (re-verified on Go
  1.26.7, transitively via go-finding) — revisit when a Go release graduates it.
